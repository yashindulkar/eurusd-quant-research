# Notebooks

Notebooks are thin research interfaces, not a home for reusable business logic.
They should import tested package functions, identify registered input versions,
declare configuration, and produce outputs under `reports/`.

Do not commit large embedded outputs or copies of raw data. Exploratory and
confirmatory notebooks must be labelled clearly.
